<snippet>
  <content><![CDATA[
import React from 'react';
import { inject, observer } from 'mobx-react';
import { TPage } from 'types';

const ${1:Page}: React.SFC<TPage> = () => {
  return (
    ${2:<div></div>}
  );
};

export default inject('store')(observer(${1:Page}));

]]></content>
  <tabTrigger>rcfp</tabTrigger>
  <description>带 mobx HOC的页面组件</description>
</snippet>